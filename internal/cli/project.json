{
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc -b",
        "cwd": "internal/cli"
      }
    },
    "gen": {
      "executor": "nx:run-commands",
      "options": {
        "command": "node lib/gen",
        "cwd": "internal/cli"
      },
      "dependsOn": [
        "build"
      ]
    }
  }
}
