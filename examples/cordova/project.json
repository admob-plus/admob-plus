{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "targets": {
    "test-android": {
      "executor": "nx:run-commands",
      "options": {
        "commands": [
          "is-ci && cordova platform rm ios || true",
          "is-ci && cordova platform rm browser || true",
          "cordova prepare --verbose"
        ],
        "cwd": "examples/cordova",
        "parallel": false
      }
    },
    "test-ios": {
      "executor": "nx:run-commands",
      "options": {
        "commands": [
          "is-ci && cordova platform rm android || true",
          "is-ci && cordova platform rm browser || true",
          "cordova prepare --verbose"
        ],
        "cwd": "examples/cordova",
        "parallel": false
      }
    }
  }
}
